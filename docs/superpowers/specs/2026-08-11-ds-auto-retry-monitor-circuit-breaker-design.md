# DS 自动重跑监控与国家级熔断设计

## 背景

当前六国 DS 失败自动重跑由各国服务器上的独立后台进程执行。每个失败实例调用
`START_FAILURE_TASK_PROCESS` 从失败节点继续执行，并轮询原工作流实例状态。

现有实现缺少两个边界：

- 单实例可能长时间保持运行或未知状态，占用后台进程并持续请求 DS API。
- 同一国家发生批量失败时，每个实例都会独立重跑和监控，可能进一步增加异常 DS 的压力。

本设计增加单实例 30 分钟生命周期与国家级并发熔断，同时保留成功、三次明确失败和准确任务日志通知。

## 目标

- 单实例自动处理总时长最多 30 分钟。
- 30 分钟内成功时必须发送恢复成功通知。
- 30 分钟内三次重跑均明确失败时立即发送最终失败通知，不等待超时。
- 30 分钟到期仍未成功时停止处理并发送人工介入通知。
- 同一国家当前活跃自动重跑/监控实例数达到 10 时，停止该国全部监控并只发送一次 DS 不健康通知。
- 国家级熔断保持 30 分钟，到期且活跃实例数低于 10 时自动解除。
- 六国共享同一套实现，但状态、计数和熔断互相隔离。

## 非目标

- 不修改 DolphinScheduler 工作流定义。
- 不引入 Redis、数据库或新的常驻服务。
- 不跨国家共享活跃实例计数。
- 不强制终止 DS 中已经启动的工作流实例；停止的是自动重跑和监控进程。

## 总体方案

采用“国家级本地注册表 + 文件锁”。每个国家服务器维护独立 JSON 注册表，所有自动重跑进程通过
`flock` 原子地登记、更新心跳、读取熔断状态和注销。

相比扫描系统进程，该方案可以准确关联国家、工作流实例和请求；相比 Redis 或数据库，该方案不增加外部依赖，符合当前每国独立服务器的部署方式。

## 单实例生命周期

单实例生命周期从后台进程取得实例锁并登记为活跃监控时开始，统一设置 30 分钟截止时间。轮询间隔默认 60 秒，最后一次休眠不得超过剩余时间，因此超时判断最多只产生一个很小的调度误差。

以下条件按照先发生者终止自动处理：

### 1. 30 分钟内成功

- 工作流实例状态变为 `SUCCESS`。
- 清理该实例的重跑计数和失败上下文。
- 从国家级活跃注册表注销。
- 发送“自动重跑已恢复成功”，包含原失败任务、原失败原因和实际重跑次数。

### 2. 30 分钟内三次重跑均明确失败

- 每轮 `retry_instance` 最多执行一次。
- 重跑动作被接受后，监控到终态失败，记为一次明确失败。
- 重跑动作本身失败时也记为一次失败，并在通知中保留 `RETRY_ACTION_FAILED` 状态。
- 累计三次失败后立即停止，不等待 30 分钟到期。
- 重新读取最后失败任务日志；优先使用任务级失败原因和任务名。
- 发送最终失败通知，包含任务名、准确失败原因、重跑次数、当前状态及“需要负责人查看”。

### 3. 30 分钟仍未成功

- 即使尚未完成三次明确失败，只要总生命周期到达 30 分钟就停止。
- 适用于运行中、等待中、临时 `UNKNOWN`、只失败一至两次或重跑动作未完成等状态。
- 不再启动新的重跑，也不再继续轮询。
- 从缓存和最新可用任务日志组合出任务名与失败原因。
- 发送超时人工处理通知，包含任务名、失败原因、实际重跑次数、当前状态及“需要负责人查看”。

## 国家级活跃注册表

每个国家使用独立文件，例如：

```text
auto_repair_records/pk_ds_failed_active_monitors.json
```

注册表包含：

```json
{
  "circuit_open_until": "2026-08-11T16:30:00+08:00",
  "circuit_alert_sent_at": "2026-08-11T16:00:00+08:00",
  "monitors": {
    "pk:169585666733760:2367606": {
      "pid": 12345,
      "request_id": "pk-ds-alert-...",
      "instance_id": "2367606",
      "started_at": "2026-08-11T16:00:00+08:00",
      "last_heartbeat_at": "2026-08-11T16:05:00+08:00"
    }
  }
}
```

所有读写操作必须在国家级文件锁内完成。进程在每次状态查询前后更新心跳，并在任何正常退出路径通过 `finally` 注销。

异常退出记录按以下规则清理：

- PID 已不存在；或
- 心跳超过 5 分钟且无法确认对应进程仍存活。

## 国家级熔断

### 触发

- 新实例完成登记后，或现有实例更新心跳时，清理失效记录并计算当前活跃实例数。
- 活跃实例数达到 10（包含第 10 个）时，原子设置 `circuit_open_until = 当前时间 + 30 分钟`。
- 只有成功创建本轮熔断的进程发送一次国家级通知。

通知包含：

- 国家；
- 当前活跃失败实例数；
- DS 状态不健康；
- 已停止该国全部自动重跑和监控；
- 熔断预计解除时间；
- 需要负责人检查 DS 服务、队列和工作流失败情况。

### 熔断期间

- 已存在监控在下一次心跳时发现熔断并退出，不再调用 `retry_instance` 或 `get_instance`。
- 第 10 个触发熔断的进程同样退出。
- 新告警只读取熔断状态并退出，不启动自动重跑、不进入监控、也不重复发送健康告警。
- 国家级健康告警优先于各实例的 30 分钟或三次失败通知，避免同时产生十条以上重复人工告警。

### 自动解除

- 熔断至少保持 30 分钟。
- 到期后的第一个新请求或状态检查在文件锁内清理失效记录。
- 若活跃实例数低于 10，则清除熔断时间和本轮通知标记，允许新实例进入。
- 若仍达到 10，则将熔断延长 30 分钟；同一连续熔断周期不重复通知。

## 状态查询和重跑规则

- `START_FAILURE_TASK_PROCESS` 仍由 `retry_instance` 发起，从失败节点继续执行。
- 每轮重跑前读取失败任务列表和任务日志，保存任务名与准确失败原因。
- 重跑运行期间只轮询原工作流实例状态，不重复调用 `retry_instance`。
- 临时 `UNKNOWN` 视为查询异常，继续轮询；它本身不触发新一轮重跑。
- 只有明确终态失败且仍在 30 分钟期限内，才进入下一轮重跑。
- 总重跑次数上限保持为 3。

## 配置

默认配置：

```text
DS_FAILED_MAX_RETRIES=3
DS_FAILED_INSTANCE_TIMEOUT_SECONDS=1800
DS_FAILED_MONITOR_INTERVAL_SECONDS=60
DS_FAILED_COUNTRY_ACTIVE_LIMIT=10
DS_FAILED_COUNTRY_CIRCUIT_SECONDS=1800
DS_FAILED_MONITOR_STALE_SECONDS=300
```

现有 `DS_FAILED_MONITOR_TIMEOUT_SECONDS` 迁移为 `DS_FAILED_INSTANCE_TIMEOUT_SECONDS`；兼容期内若新变量未设置，可以读取旧变量，但默认值改为 1800 秒。

## 通知规范

### 恢复成功

```text
原失败任务：<task_name>
定时任务原失败原因：<task_log_reason>
自动重跑已恢复成功，重跑次数：<attempts>
```

### 三次明确失败

```text
失败任务：<task_name>
定时任务执行失败，失败原因：<latest_task_log_reason>
自动重跑已完成 3 次且全部失败，当前状态：<state>，需要负责人查看
```

### 30 分钟未恢复

```text
失败任务：<task_name>
定时任务 30 分钟内未恢复，失败原因：<best_available_reason>
已停止自动重跑和监控，实际重跑次数：<attempts>，当前状态：<state>，需要负责人查看
```

### 国家级熔断

```text
<country_name> DolphinScheduler 当前状态不健康
当前活跃失败重跑/监控实例数：<active_count>
已停止该国全部自动重跑和监控，熔断 30 分钟，需要负责人查看
```

## 错误处理

- 注册表 JSON 损坏时先保留损坏文件副本，再以空注册表恢复；同时写入后台日志。
- 注册表锁获取失败时采用保守策略：不启动新的自动重跑，并在后台日志中记录原因。
- TV 通知发送失败不改变已经做出的停止或熔断决定，返回结果中保留通知失败信息。
- DS 查询短暂失败产生 `UNKNOWN` 时继续轮询，但仍受 30 分钟总截止时间限制。

## 测试标准

必须新增并通过以下测试：

1. `RUNNING → SUCCESS`：30 分钟内发送运行中和恢复成功通知，只重跑一次。
2. `RUNNING → UNKNOWN → SUCCESS`：临时未知状态不触发重复重跑。
3. 三次终态失败：第三次失败后立即通知，不等待 30 分钟。
4. 30 分钟超时：未完成三次失败也停止，并包含任务名、原因、次数和状态。
5. 活跃数从 9 增加到 10：只创建一次国家级熔断并只发送一次健康告警。
6. 已有监控发现熔断：停止轮询并注销，不发送实例级重复告警。
7. 熔断期间新告警：不调用重跑和状态查询，不重复通知。
8. 熔断到期且活跃数低于 10：自动解除并允许新实例。
9. 熔断到期但活跃数仍达到 10：延长熔断且不重复通知。
10. 进程异常退出：失效注册记录被清理，不永久占用名额。
11. 六国状态文件隔离：一个国家熔断不影响其他国家。
12. 现有失败原因提取、任务名通知、重跑计数和防重复锁测试保持通过。

## 验收标准

- 单实例自动处理不会超过 30 分钟。
- 成功、三次明确失败和 30 分钟未恢复三条路径均有且仅有一条最终通知。
- 同国活跃实例达到 10 时，该国全部监控停止且只产生一条国家级通知。
- 熔断 30 分钟后仅在活跃数低于 10 时自动恢复。
- 国家级熔断期间不会新增 DS 重跑或监控请求。
- 全量自动化测试、Python 编译检查和 `git diff --check` 通过。
